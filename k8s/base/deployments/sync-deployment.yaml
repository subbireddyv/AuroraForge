apiVersion: apps/v1
kind: Deployment
metadata:
  name: auroraforge-sync
  namespace: auroraforge
  labels:
    app.kubernetes.io/name: auroraforge-sync
    app.kubernetes.io/part-of: auroraforge
    app.kubernetes.io/component: sync
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: auroraforge-sync
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: auroraforge-sync
        app.kubernetes.io/part-of: auroraforge
        app.kubernetes.io/component: sync
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8093"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      serviceAccountName: auroraforge-sync
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      terminationGracePeriodSeconds: 60
      containers:
        - name: sync
          image: auroraforge/auroraforge-sync:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8083
              protocol: TCP
            - name: actuator
              containerPort: 8093
              protocol: TCP
          envFrom:
            - configMapRef:
                name: auroraforge-common
            - configMapRef:
                name: auroraforge-sync-config
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "k8s"
            - name: DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: auroraforge-db-secret
                  key: password
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "2Gi"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8093
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8093
            initialDelaySeconds: 20
            periodSeconds: 5
            failureThreshold: 3
            timeoutSeconds: 3
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 5"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: logs
              mountPath: /logs
      volumes:
        - name: tmp
          emptyDir: {}
        - name: logs
          emptyDir: {}
